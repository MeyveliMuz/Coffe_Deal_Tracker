fastapi>=0.110
uvicorn[standard]>=0.29
apscheduler>=3.10
playwright>=1.45
# NOT: PySide6 backend için GEREKLİ DEĞİL — yalnızca masaüstü (Qt) istemcisi kullanır.
# Container'da tarayıcı: `playwright install --with-deps chromium` (bkz. backend/Dockerfile).
