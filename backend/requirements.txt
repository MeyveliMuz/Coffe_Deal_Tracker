fastapi>=0.110
uvicorn[standard]>=0.29
apscheduler>=3.10
# Çekirdek bağımlılıkları (scraping/depolama) köke ait requirements.txt'tedir:
#   playwright>=1.45
# PySide6 backend için GEREKLİ DEĞİL — yalnızca masaüstü (Qt) istemcisi kullanır.
