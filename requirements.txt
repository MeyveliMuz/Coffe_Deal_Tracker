# Masaüstü wrapper (desktop.py) + web backend için Python bağımlılıkları.
# (Frontend bağımlılıkları frontend/package.json içindedir.)
PySide6>=6.7            # QtWebEngine penceresi — masaüstü wrapper
playwright>=1.45       # site tarama (Chromium)
fastapi>=0.110
uvicorn[standard]>=0.29
apscheduler>=3.10
